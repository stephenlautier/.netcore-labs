version: "3"

services:
  api:
    image: slabs.simpledockerapi
    build:
      context: ./Slabs.SimpleDockerApi
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5010:80"
